# Task-1-MeghaSoni

```
AI Chatbot

Description
A rule-based chatbot built in Python. It responds to greetings, small talk, and basic questions using simple if-else/dictionary logic — no machine learning involved. Runs in a continuous loop until the user types an exit command.

How to Run
python rule_based_chatbot.py

No installation needed — pure Python, no external libraries.

```
